# 首单客户试点驻场检查清单

> **版本**：PoC Freeze v1  
> **适用场景**：1 个试点区域 · 1–2 名门卫 · 2 天试运行  
> **目标**：连续 2 个工作日无 P0 问题，客户签字验收  
> **填写人**：\_\_\_\_\_\_\_\_\_\_ **日期**：\_\_\_\_\_\_\_\_\_\_

---

## Day 0 — 驻场前远程准备

### 物料确认

- □ 拿到工厂网络信息：WiFi SSID / 密码 / 服务器 IP 段
- □ 确认工厂路由器是否存在 AP 隔离（客户端隔离）
- □ 准备测试手机 ≥ 4 台（小米 / OPPO / vivo / Samsung 各一）
- □ APK 已编译并放入 `public/factory-control.apk`
- □ `.env` 已配置 JWT\_SECRET 和 SUPER\_ADMIN\_PASSWORD（非默认值）
- □ 部署文档（DEPLOYMENT.md）已打印或存入手机
- □ 本清单已打印

### 账号预置

- □ 超管邮箱 / 密码已记录：\_\_\_\_\_\_\_\_\_\_ / \_\_\_\_\_\_\_\_\_\_
- □ 试点厂区名称已与客户确认：\_\_\_\_\_\_\_\_\_\_
- □ 试点区域名称已与客户确认：\_\_\_\_\_\_\_\_\_\_

---

## Day 1 上午 — 服务器部署与基础验证

### 1. 服务器检查

- □ 操作系统为 Ubuntu 22.04 LTS
- □ 服务器已分配固定内网 IP：\_\_\_\_\_\_\_\_\_\_
- □ 磁盘可用空间 ≥ 10 GB
- □ 系统时间正确（`date` 输出与北京时间一致）

### 2. Node / PM2 / SQLite 检查

- □ `node -v` 输出 ≥ 18.0.0：\_\_\_\_\_\_\_\_\_\_
- □ `npm -v` 输出正常：\_\_\_\_\_\_\_\_\_\_
- □ `pm2 -v` 输出正常：\_\_\_\_\_\_\_\_\_\_
- □ `npm install --production` 无报错（特别是 better-sqlite3 编译成功）
- □ `data/` 和 `logs/` 目录已创建且有写权限
- □ `npm run db:migrate` 执行成功

### 3. 启动与进程确认

- □ `pm2 start ecosystem.config.js --env production` 无报错
- □ `pm2 status` 显示 factory-saas 为 `online`
- □ `pm2 save` 已执行
- □ `pm2 startup` 已配置（重启后自动恢复）
- □ `ss -tlnp | grep 3000` 显示 LISTEN
- □ `pm2 logs factory-saas --lines 20` 无 ERROR / ❌ 字样
- □ 日志中出现 `[DB] 已应用迁移` 和超管创建信息

### 4. Dashboard 页面检查

- □ 服务器本机 `curl -s http://localhost:3000/ -o /dev/null -w "%{http_code}"` 返回 302
- □ 浏览器打开 `http://<IP>:3000/login` 页面正常加载
- □ 用超管账号登录成功
- □ 跳转到 Dashboard，页面无白屏 / JS 报错
- □ 左上角显示厂区名称
- □ 右下角连接状态指示灯为绿色（WebSocket 已连接）
- □ 创建试点区域成功（Dashboard → 添加区域）
- □ 区域下拉列表中可选到新创建的区域

### 5. APK 下载检查

- □ `ls -lh public/factory-control.apk` 文件存在，大小合理（10–30 MB）
- □ `curl -sI http://localhost:3000/factory-control.apk | grep Content-Length` 返回正确大小
- □ 服务器本机 `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/factory-control.apk` 返回 200

---

## Day 1 下午 — 网络与真机验证

### 6. WiFi 和局域网检查

- □ 服务器已接入工厂网络（有线 / WiFi）
- □ 手机连接工厂 WiFi：SSID = \_\_\_\_\_\_\_\_\_\_
- □ 手机获取到的 IP 与服务器在同一网段
  - 服务器 IP：\_\_\_\_\_\_\_\_\_\_ 手机 IP：\_\_\_\_\_\_\_\_\_\_
- □ 手机浏览器能打开 `http://<IP>:3000/login`
- □ 手机浏览器能打开 `http://<IP>:3000/welcome-bridge`
- □ 从 welcome-bridge 点击下载 APK 成功
- □ APK 安装成功（需允许"未知来源"）

### 7. WebSocket 检查

- □ Dashboard 页长时间保持（≥ 5 分钟）连接状态灯绿色
- □ 新建一条入场会话 → Dashboard 实时刷新显示新行
- □ 会话状态变化后 Dashboard 自动更新（无需手动刷新）

### 8. Android 真机兼容检查

每台测试手机完成以下全流程，记录结果：

#### 小米（Xiaomi / Redmi / POCO）

- □ 型号：\_\_\_\_\_\_\_\_\_\_ 系统版本：\_\_\_\_\_\_\_\_\_\_
- □ APK 安装成功
- □ App 启动正常 → 显示空闲扫码界面
- □ 扫入场码后进入"正在为您办理入场"
- □ 自动化完成 → 进入管控状态（RESTRICTED）
- □ 摄像头已被禁用（打开相机 App 验证黑屏 / 无法拍照）
- □ 扫离场码 → 管控解除 → 相机恢复正常
- □ 结果：通过 / 不通过 \_\_\_\_\_\_\_\_\_\_ 备注：\_\_\_\_\_\_\_\_\_\_

#### OPPO（OPPO / Realme / OnePlus）

- □ 型号：\_\_\_\_\_\_\_\_\_\_ 系统版本：\_\_\_\_\_\_\_\_\_\_
- □ APK 安装成功
- □ App 启动正常 → 显示空闲扫码界面
- □ 扫入场码后进入"正在为您办理入场"
- □ 自动化完成 → 进入管控状态（RESTRICTED）
- □ 摄像头已被禁用（打开相机 App 验证黑屏 / 无法拍照）
- □ 扫离场码 → 管控解除 → 相机恢复正常
- □ 结果：通过 / 不通过 \_\_\_\_\_\_\_\_\_\_ 备注：\_\_\_\_\_\_\_\_\_\_

#### vivo（vivo / iQOO）

- □ 型号：\_\_\_\_\_\_\_\_\_\_ 系统版本：\_\_\_\_\_\_\_\_\_\_
- □ APK 安装成功
- □ App 启动正常 → 显示空闲扫码界面
- □ 扫入场码后进入"正在为您办理入场"
- □ 自动化完成 → 进入管控状态（RESTRICTED）
- □ 摄像头已被禁用（打开相机 App 验证黑屏 / 无法拍照）
- □ 扫离场码 → 管控解除 → 相机恢复正常
- □ 结果：通过 / 不通过 \_\_\_\_\_\_\_\_\_\_ 备注：\_\_\_\_\_\_\_\_\_\_

#### Samsung（三星）

- □ 型号：\_\_\_\_\_\_\_\_\_\_ 系统版本：\_\_\_\_\_\_\_\_\_\_（One UI \_\_\_\_）
- □ APK 安装成功
- □ App 启动正常 → 显示空闲扫码界面
- □ 扫入场码后进入"正在为您办理入场"
- □ 自动化完成 → 进入管控状态（RESTRICTED）
- □ 摄像头已被禁用（打开相机 App 验证黑屏 / 无法拍照）
- □ 扫离场码 → 管控解除 → 相机恢复正常
- □ 结果：通过 / 不通过 \_\_\_\_\_\_\_\_\_\_ 备注：\_\_\_\_\_\_\_\_\_\_

### 兼容性汇总

| 品牌 | 型号 | 系统 | 入场 | 管控 | 离场 | 结论 |
|------|------|------|------|------|------|------|
| 小米 | | | □ | □ | □ | |
| OPPO | | | □ | □ | □ | |
| vivo | | | □ | □ | □ | |
| Samsung | | | □ | □ | □ | |

> **判定标准**：4 个品牌中 ≥ 3 个全流程通过即可进入试运行。Samsung 如存在已知问题须在备注中记录并告知客户。

---

## Day 1 傍晚 — 门卫培训

### 9. 门卫培训完成确认

- □ 门卫 A 姓名：\_\_\_\_\_\_\_\_\_\_ 培训完成 □
- □ 门卫 B 姓名：\_\_\_\_\_\_\_\_\_\_ 培训完成 □

#### 培训内容勾选

- □ 门卫能独立登录 Dashboard
- □ 门卫理解"入场二维码"与"离场二维码"的区别
- □ 门卫会在 Dashboard 上为访客办理入场（点击区域 → 生成入场码）
- □ 门卫知道让访客扫入场码后等待 App 自动完成设置
- □ 门卫能识别 App 上"管控已生效"的界面状态
- □ 门卫知道访客离场时需扫离场码
- □ 门卫知道遇到问题时联系实施工程师的方式：\_\_\_\_\_\_\_\_\_\_
- □ 门卫知道 techMode 长按切换操作（仅在实施工程师指导下使用）
- □ 门卫知道当 App 提示"如需帮助请联系门卫"时该怎么做

---

## Day 1 结束 — 试运行前确认

### 10. Day 1 收尾检查

- □ 服务器进程运行正常（`pm2 status` = online）
- □ 数据库备份已执行一次（`npm run db:backup`）
- □ 所有测试会话已在 Dashboard 中标记为"已离场"
- □ 无残留的 WAITING / RESTRICTED 状态测试会话
- □ 真机测试中发现的问题已记录并评估严重性
- □ P0 问题数量：\_\_\_\_\_\_（必须为 0 才进入 Day 2 试运行）
- □ 已与客户确认明天的试运行安排（来访客时间段、预计人数）

---

## Day 2 上午 — 真实访客试运行

### 11. 入场流程实战验证

- □ 服务器在过夜后仍正常运行（`pm2 status` = online）
- □ 日志无异常（`pm2 logs factory-saas --lines 50` 检查）
- □ Dashboard WebSocket 连接正常（绿色指示灯）

**第 1 位真实访客：**

- □ 姓名：\_\_\_\_\_\_\_\_ 手机品牌 / 型号：\_\_\_\_\_\_\_\_\_\_
- □ 门卫在 Dashboard 生成入场码
- □ 访客手机扫码后 App 自动完成设置
- □ Dashboard 实时显示会话状态变为 `restricted`
- □ 访客确认相机无法使用
- □ 入场总耗时（从扫码到管控生效）：\_\_\_\_ 分 \_\_\_\_ 秒
- □ 访客体验反馈：\_\_\_\_\_\_\_\_\_\_

**第 2 位真实访客（如有）：**

- □ 姓名：\_\_\_\_\_\_\_\_ 手机品牌 / 型号：\_\_\_\_\_\_\_\_\_\_
- □ 全流程完成 □ 异常记录：\_\_\_\_\_\_\_\_\_\_
- □ 入场总耗时：\_\_\_\_ 分 \_\_\_\_ 秒

### 12. 离场流程实战验证

- □ 门卫在 Dashboard 展示离场二维码
- □ 访客手机扫离场码 → App 显示"正在办理离场"
- □ App 显示"离场已办理，感谢配合"
- □ Dashboard 会话状态变为 `exited`
- □ 访客确认相机已恢复正常
- □ 离场总耗时（从扫码到管控解除）：\_\_\_\_ 秒

### 异常场景验证（门卫操作）

- □ 访客在管控中再次扫入场码 → App 提示"当前正在使用中"并拒绝
- □ 断网后重连 → WebSocket 自动重连，会话状态不丢失
- □ App 被后台杀死后重新打开 → 会话自动恢复（如在 12 小时内）

---

## Day 2 下午 — 收尾与验收

### 13. 系统稳定性确认

- □ 连续运行 ≥ 24 小时无自动重启（`pm2 status` restart 次数 = 0）
- □ 内存占用稳定（`pm2 monit` 无持续增长趋势）
- □ 数据库大小合理（`ls -lh data/factory.db`）
- □ 自动备份正常执行（`ls -lt data/backups/ | head -5` 有备份文件）

### 14. 客户验收前确认项

- □ 试运行期间 P0 问题数量：\_\_\_\_\_\_（必须为 0）
- □ P1 问题已记录并有解决计划：\_\_\_\_\_\_
- □ 门卫能独立完成入场 + 离场操作（不依赖实施工程师）
- □ 客户指定的信息安全负责人已知晓系统运作方式
- □ 超管账号已交接给客户指定管理人
  - 邮箱：\_\_\_\_\_\_\_\_\_\_ 已确认可登录 □
- □ 实施工程师联系方式已留给门卫和管理人
- □ 系统维护说明（重启方法、日志查看、备份恢复）已口头交代

### 客户签字

| 项目 | 确认 |
|------|------|
| 服务器部署完成 | □ |
| 真机兼容性达标（≥ 3/4 品牌通过） | □ |
| 入场 + 离场全流程跑通 | □ |
| 门卫培训完成 | □ |
| 连续运行无 P0 | □ |

**客户签字**：\_\_\_\_\_\_\_\_\_\_ **日期**：\_\_\_\_\_\_\_\_\_\_  
**实施工程师签字**：\_\_\_\_\_\_\_\_\_\_ **日期**：\_\_\_\_\_\_\_\_\_\_

---

## 问题记录表

| 编号 | 时间 | 描述 | 品牌/型号 | 严重性 | 状态 | 备注 |
|------|------|------|-----------|--------|------|------|
| 1 | | | | P0/P1/P2 | | |
| 2 | | | | P0/P1/P2 | | |
| 3 | | | | P0/P1/P2 | | |
| 4 | | | | P0/P1/P2 | | |
| 5 | | | | P0/P1/P2 | | |

> **P0**：流程阻断，无法入场或无法管控。必须当场修复。  
> **P1**：功能异常但有 workaround。须在 1 周内修复。  
> **P2**：体验问题。纳入后续迭代。
